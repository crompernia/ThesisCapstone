# Database Configuration
# For local development with Docker Compose
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/thesis_db

# For production with Neon (Recommended)
# Get from: https://console.neon.tech → Your Project → Connection Details
# DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# For migrations with Neon (optional but recommended)
# Use direct connection for schema changes
# DIRECT_DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require&connect_timeout=10

# For production with Vercel Postgres (Alternative)
# Vercel will automatically set POSTGRES_URL when you connect the database
# DATABASE_URL=${POSTGRES_URL}

# NextAuth Configuration
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32

# For local development
NEXTAUTH_URL=http://localhost:9002

# For production (Vercel will auto-set this, or set manually)
# NEXTAUTH_URL=https://your-app.vercel.app

# Google AI (Genkit)
# Get your API key from: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
